# Invoice Creator

This portfolio include:
* React
* Material ui
* Babel
* React addons update
* Css-loader
* Style-loader
* Webpack

In the project's directory, run the following commands:

```
$ npm install
$ npm start
```
visit
```
http://localhost:7777
```